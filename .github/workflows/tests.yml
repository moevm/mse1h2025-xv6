name: CI Tests

on: [push, pull_request]

jobs:
  run-tests:
    runs-on: ubuntu-latest
    env:
      PYTHONPATH: ${{ github.workspace }}
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y make gcc qemu-system-x86 
    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov

    - name: Configure git
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
        git config --global init.defaultBranch main
        
    - name: Verify installations
      run: |
        make --version
        gcc --version
        qemu-system-x86 --version
        
    - name: Debug info
      run: |
        ls -lR lab_ready
        which qemu-system-x86
        qemu-system-x86 --version

    - name: Find QEMU
      run: |
        which qemu-system-x86 || echo "QEMU not found!"
    
    - name: Run tests
      run: |
        pytest tests/functional/ tests/integration/ -v --cov=scripts --cov-report=xml
        
    - name: Upload coverage
      uses: codecov/codecov-action@v3
